services:
  - type: web
    name: wb-sync
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node sync-server.js
    envVars:
      - key: PORT
        value: 10000
      - key: SYNC_TOKEN
        generateValue: true
